# Sixth Field — Players Watch (Live RSS)
- `/api/posts/rss` fetches live RSS from BAE, Anduril, Thales, Lockheed, RTX, QinetiQ, Drumgrange
- `/api/plan/pipeline?preset=standard|linkedin|xfast` builds weekly plan
- `/api/export/csv-scheduler` exports CSV for Buffer/Hootsuite
- Control panel at `/tools/form`

Deploy on Vercel → open `/tools/form`.
